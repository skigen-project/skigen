# This file exports the Skigen::Skigen CMake target which should be passed
# to the target_link_libraries command.

@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
find_dependency(Eigen3 3.4)

if(NOT TARGET Skigen::Skigen)
    include("${CMAKE_CURRENT_LIST_DIR}/SkigenTargets.cmake")
endif()
